import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html', // Указываем путь к HTML-файлу
  styleUrls: ['./app.component.css'] // Указываем путь к CSS-файлу (если есть)
})
export class AppComponent {
  title = 'cookbook-app';
}
